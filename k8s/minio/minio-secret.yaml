apiVersion: v1
kind: Secret
metadata:
  name: minio-secret
  namespace: file-sharing
type: Opaque
data:
  # Base64 encoded MinIO credentials
  # To encode: echo -n "your-value" | base64
  
  # Default credentials (CHANGE IN PRODUCTION!)
  root_user: bWluaW9hZG1pbg==  # minioadmin
  root_password: bWluaW9hZG1pbg==  # minioadmin
  
  # Note: In production, create this secret with actual credentials:
  # kubectl create secret generic minio-secret \
  #   --from-literal=root_user=admin \
  #   --from-literal=root_password=your-secure-password \
  #   -n file-sharing

